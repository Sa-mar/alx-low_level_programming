#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * int _abs - The prototype
 *
 * Description: a function that computes the absolute value of an integer
 *
 * Return: Always o
 */
int _abs(int)
{
	int n;

	if (n == 0)
	{
		_putchar(n);
	}
	else if (n > 0)
	{
		_putchar(n);
	}
	else
	{
		_putchar((n * -1) + '0');
	}
	return (0)
}
