#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _isdigit - The function
 * @c: The value to check
 *
 * Description: function that check for a digit (0 through 9)
 *
 * Return: 1 if digit otherwise 0
 */
int _isdigit(int c)
{
	if (c = >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
