#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * _strlen - The function
 * @s: the pointer
 *
 * Description: A function to return lengh of string
 *
 * Return: The length
 */
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
	}
	return (count);
}
